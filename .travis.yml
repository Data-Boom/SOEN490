sudo: required

language: node_js

node_js:
  - "14"

services:
   - docker

branches:
  only:
    - develop
    - ci-integration-44

script:
    - cd Server && npm install && npm test
    - cd ../Client && npm install
    - cd ../Server
    - docker-compose up -d --build

after_script:
- docker-compose down