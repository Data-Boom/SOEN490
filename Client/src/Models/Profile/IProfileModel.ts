//todo remove there is a model in the authentication folder

export interface IUser {
  name: string,
  email: string,
  orcID: number,
  //dateOfBirth: string,
  organization: string,
  password: string
}