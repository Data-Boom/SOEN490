export interface IDataRequestModel {
    datasetId: number[]
    material: string[]
    firstName: string
    lastName: string
    year: number
    categoryId: number
    subcategoryId: number
    datapoints: string[]
}
